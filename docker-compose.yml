services:
  afk-bot:
    build: .
    container_name: aternos-afk-bot
    restart: unless-stopped
    environment:
      - MC_HOST=VenusWorldSMP.aternos.me
      - MC_PORT=50629
      - MC_USERNAME=Venus Bot
    volumes:
      # Persist Microsoft auth tokens so you don't have to re-login
      - ./auth:/app/auth
    stdin_open: true
    tty: true
